<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Accueil</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
  
<body class="bg-[#FAF3E5] min-h-screen flex flex-col">
    <%- include('partials/head.ejs') %>
    <%- include('partials/header.ejs') %>
    <main class="flex-1 flex flex-col justify-center items-center">
        <div class="mx-auto">
            <img src="logoargos.png" alt="Logo Argos"
                class="object-contain max-w-full max-h-full sm:max-w-xs md:max-w-md lg:max-w-lg xl:max-w-xl"
                style="transition: transform 0.2s;">
        </div>
        <button
            class="mt-8 px-8 py-3 rounded-full border-2 border-[#5C83BA] text-[#5C83BA] bg-transparent hover:bg-[#5C83BA] hover:text-white transition-colors duration-200"
        >
            Commencer
        </button>
    </main>
    <script>
        document.querySelector('button').onclick = function() {
            window.location.href = 'connexion';
        };
    </script>
</body>
</html>